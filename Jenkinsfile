pipeline {
 agent any
  stages {
    stage('build') {
         steps {
           sh 'docker build -t myapp1 .'

          }
        }
      stage('push image') {
              steps {
                sh 'docker build -t myapp1 .'

     }
 }
